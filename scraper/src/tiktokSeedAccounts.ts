// Discovery for TikTok is seed-list based, not keyword search — see the
// comment in tiktok.ts for why. Each handle below was checked live to
// resolve to a real profile before being added here.
//
// Edit this list to grow coverage. "alexhormozi" returned an ambiguous
// generic-title page during verification (possibly a wrong handle, possibly
// a transient block) and was left out rather than guessed at.
export const TIKTOK_SEED_HANDLES = [
  "danmartell",
  "noahkagan",
  "gregisenberg",
  "davidondrej1",
  "levelsio",
  "codiesanchez",
  "hustlefund",
];
